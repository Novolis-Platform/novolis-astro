# Release

Packages publish as `Novolis.Astro.*` on GitHub Packages under the `2026.1.*` line on merge to `main`.

Version comes from `build/version.json` + CI build number via Novolis governance version targets.
